# Core LLM parsing stack
openai>=1.30.0
azure-identity>=1.16.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# SOCKS proxy support (needed when running behind a socks5 proxy)
httpx[socks]>=0.27.0
PySocks>=1.7.1

# Graph workflow demo
langgraph>=0.2.0

# Sandbox/test tooling
docker>=7.0.0
pytest>=8.0.0
pytest-json-report>=1.5.0
